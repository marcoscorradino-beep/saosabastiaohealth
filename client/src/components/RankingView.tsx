interface Row { ine:string; name:string; value:number|null; classification:string; }
export default function RankingView({ rows }:{rows:readonly Row[]}) { const ranked=[...rows].filter(r=>r.value!=null).sort((a,b)=>(b.value??0)-(a.value??0)); return <div className="bg-white rounded-xl border shadow-sm overflow-hidden">{ranked.map((r,i)=><div key={r.ine} className="flex items-center gap-4 px-5 py-3 border-b last:border-0"><div className="w-8 h-8 rounded-full bg-blue-50 text-blue-800 flex items-center justify-center font-bold">{i+1}</div><div className="flex-1"><p className="font-semibold">{r.name}</p><p className="text-xs text-gray-500">{r.classification}</p></div><div className="text-lg font-bold text-blue-800">{r.value?.toFixed(2)}%</div></div>)}</div> }
