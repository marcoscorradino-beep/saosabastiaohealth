interface Row { ine:string; cnes:string; establishment:string; name:string; teamType:string; value:number|null; classification:string; }
export default function DataTable({ rows }:{rows:readonly Row[]}) {
 return <div className="overflow-hidden bg-white rounded-xl border border-gray-200 shadow-sm"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-50 border-b"><tr>{["Equipe","INE","Estabelecimento","Resultado","Classificação"].map(h=><th key={h} className="px-5 py-3 text-left font-semibold">{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r.ine} className={`border-b ${i%2?"bg-gray-50/60":"bg-white"}`}><td className="px-5 py-3 font-medium">{r.name}</td><td className="px-5 py-3 text-gray-600">{r.ine}</td><td className="px-5 py-3 text-gray-600">{r.establishment}</td><td className="px-5 py-3 font-bold">{r.value==null?"—":`${r.value.toFixed(2)}%`}</td><td className="px-5 py-3"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold">{r.classification||"—"}</span></td></tr>)}</tbody></table></div></div>
}
